# klide app tracking

The app itself, restarted from a simulator because the device is not here and a rendered frame is the evidence
an agent can check its own work against. Analysis and decisions in [`00_start.md`](00_start.md);
the requirements it inherits are in [`../00_initial/00_start.md`](../00_initial/00_start.md) and
[`../02_kobo/`](../02_kobo/). How the work is run is the meta track, [`../03_meta/tracking.md`](../03_meta/tracking.md).

## Key decisions

- The stack is chosen against a written requirement list, not by racing prototypes (AD1).
- The simulator is headless and emits frames; the viewer is a thin layer on top (AD2).
- It imitates what can make a frame wrong or a design bad, and stands in for the client as well as the panel (AD3, AD4).
- One wire protocol for the simulator and the real device (AD5).
- The frame is the evidence, which is where this track meets the meta one (MD10).
- Python for the host, the renderer and the simulator, confirming D10 against written requirements (AD7).
- The evidence is a PNG, references are committed, and the match tolerance is zero (A5, phase 2).
- The refresh timings are FBInk's documented figures, not measurements, and calibration waits for the device (phase 3).
- The taller page buffer and the cache are one structure, a bounded cache of pages of any height (K9, phase 3).
- Text is sized in points against the panel's real 300 ppi, never in pixels (AD8).
- Colour is not translated to grey: diffs lead with their own markers, highlighting groups by prominence (phase 4).
- Fonts are vendored so references stay reproducible without a system font (phase 4).
- Every gate is self-referential, so a person looking at the screen is the one outside signal; phase 5 builds the viewer that provides it.

## Phases

| #  | Phase                      | Plan                                                          | Status  |
| -- | -------------------------- | ------------------------------------------------------------- | ------- |
| 1  | Requirements and stack     | [`01_requirements_and_stack.md`](01_requirements_and_stack.md) | done    |
| 2  | Walking skeleton           | [`02_walking_skeleton.md`](02_walking_skeleton.md)             | done    |
| 3  | Simulator fidelity         | [`03_simulator_fidelity.md`](03_simulator_fidelity.md)         | done    |
| 4  | Host renderer and views    | [`04_host_renderer.md`](04_host_renderer.md)                   | done    |
| 5  | Viewer and human feedback  | [`05_viewer.md`](05_viewer.md)                                 | in progress |
| 6  | Device client              | [`06_device_client.md`](06_device_client.md)                   | draft   |

Status values: draft / planned / in progress / done / superseded / discarded.

## Log

Append-only. Newest at the bottom.

- 2026-09-16 : folded in A1-A7 and derived the five phases. The stack is decided against written requirements rather than by a race, the simulator is headless and covers the client as well as the panel, ghosting is deferred, GPU waits for a measurement, and the evidence format is left to the first real frame comparison in phase 2
- 2026-09-16 : phase 1 done. Wrote the requirement list R1-R8 first, then scored five candidates against it in [`../../docs/stack.md`](../../docs/stack.md). Python, one language for host, renderer and simulator (AD7). The phase's real job was checking whether D10 survives being written down rather than asserted, and it does. R5 is the reason it is not Rust: D1 leaves no klide code on the device, so nothing here cross-compiles, which voids the usual argument for a compiled language. Measured the one empirical requirement, R1: a full 1264x1680 page of highlighted text renders in tens of milliseconds against a panel that answers a partial refresh in hundreds. Filled four of the five gate slots, ruff for format and lint, mypy strict, pytest, each demonstrated failing and each in `scripts/check.sh`. The lint gate found a real unused import in `scripts/gates/plan_status.py` on its first run. The frames gate stays open because A5 gives the evidence format to phase 2
- 2026-09-16 : phase 2 done. `uv run klide-skeleton` renders a page at panel size, serves it over a unix socket, receives it in the simulator, writes it out and compares it against a committed reference; the protocol is in [`../../docs/protocol.md`](../../docs/protocol.md). A5 answered: an 8-bit greyscale PNG that is lossless with respect to the 4-bit levels, references in the repo because a generated one agrees with whatever the code does, and a tolerance of zero, defensible because the host is deterministic once the font comes from pinned Pillow rather than the system. The comparison is of the received frame rather than the rendered one, so a protocol bug fails the gate instead of slipping past it. The protocol's shape was decided by the Lua client that does not exist yet: byte-aligned big-endian fields and rows padded to a byte, because KOReader's Lua has no `string.unpack`. The frames gate is filled and catches a one-pixel margin shift, which closes the last slot the meta track left open
- 2026-09-16 : phase 3 done. `uv run klide-session` drives nine steps and writes the screen after each; the client logic (bounded page cache, local panning, sleep and resume, disconnect banner) is in `src/klide/device.py`, and what the simulator claims is in [`../../docs/simulator.md`](../../docs/simulator.md). The phase's real risk was inventing latency numbers for a device that is not here. Research established that E Ink publishes no per-mode timings and that they vary with panel, controller and temperature, so FBInk's documented approximations are used and every mode carries a source string saying it was not measured on a Libra 2. Breaking the client deliberately found a gate that passed a change it should have caught: quadrupling the flash rate moves no pixels because ghosting is not modelled, so a refresh ledger is now compared alongside the images. K9 answered by building it, the taller page buffer and the cache are the same structure. The protocol moved to version 2 to carry a second message type going the other way. Dirty rectangles are supported and tested but nothing sends one yet, which is phase 4's case
- 2026-09-16 : fixed a legibility fault found by reading the frames as a physical object rather than as an image. The renderer used a 26 pixel font, which on a 300 ppi panel is 6.2 pt, about half the smallest size anyone sets a paperback in, and the point of the device is not straining to read it. Sizes are now typographic points converted through the new `Panel.ppi` (AD8): 11 pt body, 23 lines and about 51 characters a screen, 2.03 mm x-height. Two tests hold the floor, one on point size and one on lines per screen, because the mistake came from judging a 300 ppi frame on a desktop monitor and nothing structural stopped it. Raising the size then pushed every hand-wrapped line past the right edge, which the renderer clips silently, so a third test checks the canned pages fit. Both frame gates caught the change and the references were regenerated. Parked A8, how a person drives the simulator, since the viewer does not exist and the drag question is the part worth deciding deliberately
- 2026-09-16 : answered the drag half of A8, provisionally: settle on release, short one-directional drags, which is Kindle-like behaviour a reader already expects. Nothing to build, because the wire carries a discrete direction rather than a continuous delta and panning happens once per gesture; the decision records that this stays deliberate. The rest of A8, whether a viewer gets built at all and which phase owns it, is still open
- 2026-09-16 : phase 4 done. `uv run klide-views` gates all six views from fixtures; `uv run klide-live` watches a real session and streams it to the simulator, which is the first thing here that reads something nobody wrote for it. Rendering decisions in [`../../docs/rendering.md`](../../docs/rendering.md). The transcript format was read from a real file before anything was built on it, which is how the thinking finding surfaced: all 219 thinking blocks on this box carry an empty field and an opaque signature, so klide shows an activity marker because that is all the format supports. Fonts are vendored, since Pillow ships neither a monospace nor a bold. Colour is not translated to grey; diffs lead with the markers the format already has. Streaming rests on one property and that property is a test: dirty rectangles must converge on the same screen as sending the page. Running it against a real session found two defects the synthetic fixture could not, an empty heading for every tool-result turn and a heredoc rendering as a dozen lines, which is the argument for that command existing
- 2026-09-16 : added phase 5, the viewer, and pushed the device client to 6. The reason is not convenience: every gate in this repo compares klide's output against a reference klide produced, so they catch a change and cannot catch a design that was wrong when the reference was written. The agent sets the expectation and then meets it. The 6.2 pt text is the proof, since it survived three phases with every gate passing and was caught by a person asking whether the screen would be readable. A8 is now fully answered: the viewer gets built, it owns its own phase, and it comes before the device client because it needs no hardware and the feedback is worth having before anything is ported
- 2026-09-16 : settled the viewer's transport and V3, and corrected a wrong claim made getting there. The claim was that `AllowTcpForwarding` defaults to yes and was confirmed not disabled on this box; the check had grepped `/etc/ssh/sshd_config`, which does not exist here. This box runs no OpenSSH server at all, only `tailscaled`, so the session arrived over Tailscale SSH and an absent file read the same as an absent setting. It matters less than it might, because over Tailscale the viewer connects to the host's tailnet address directly and no forwarding is involved; verified that an unprivileged process can bind the tailnet IP. Where only plain SSH is available the check has to be behavioural, since reading the config needs root and `ExitOnForwardFailure` does not cover a local forward's server side. V3 answered: the viewer is one self-contained file with no `klide` import, which makes it the first implementation of the protocol written against the specification rather than sharing its code, on the condition that the repo tests its decoder against `klide`'s encoder so divergence fails a gate
- 2026-09-16 : ran the forwarding probe against the second host and it passed, so plain SSH is a usable fallback there as well as Tailscale. The banner also dated the machine, `OpenSSH_8.9p1 Ubuntu-3ubuntu0.17`, which is Ubuntu 22.04 and ships Python 3.10 against klide's 3.12 floor. Not something to ask an administrator for: uv installs its own interpreter under the user's home, which is already how this box runs, with a system Python of 3.14.4 and a venv on a 3.13.14 that uv downloaded. AD10 holds there
- 2026-09-16 : V2 answered. The viewer's refresh delay is a toggle that works mid-run, not a startup flag, and both settings stay: honest mode for judging how the device feels, fast mode for clicking between views when the question is layout. Honest is the default, because a viewer that showed an impossibly quick device would be one more agreeable mirror and AD9 is the reason the viewer exists at all. This drops an earlier plan to record which setting got used, which assumed one of them was a concession. The durations come from the refresh mode already in the frame header, through a small table in the viewer that the conformance test pins to `waveform.FBINK_CLAIMS`; a duration field on the wire was rejected because a real panel takes the time it takes and only the simulator would ever read it
- 2026-09-16 : the viewer is built and runs; the phase stays in progress because the part it exists for, a person sitting with it, has not happened. `uv run klide-live --serve` streams a live session over TCP and `viewer/klide_viewer.py` shows it, one self-contained file with no klide import. That makes it the first implementation of the wire protocol written from the specification rather than sharing the code, which is the AD5 check `docs/protocol.md` had recorded as unverified; it decoded real frames from a real host and found nothing wrong, though Lua stays unverified since a second Python implementation still shares the language's habits. The duplication is pinned to klide's by `tests/test_viewer.py`. Running it rather than testing it found two defects: the host tracebacked when the viewer closed, which is what a person does every time, and the fix then swallowed the reason silently
- 2026-09-17 : corrected the claim that the viewer needs nothing installed. It was written as verified, on the strength of this box importing tkinter from uv's CPython while its system Python could not. That held for one interpreter on one machine. The first real run, on an ordinary Ubuntu desktop rather than the WSL machine, failed with `Can't find a usable init.tcl`: uv had resolved a CPython linked against Tcl 8.6 with no 8.6 library files, where this box has 3.13 with Tcl 9.0. tkinter importing does not mean Tk works. The instructions are back to `sudo apt install python3-tk` and the system Python, `requires-python` is lowered to 3.9 so a system Python with a working Tk is eligible, and the failure now distinguishes a missing Tcl library from a missing display, since the previous single message sent someone with a perfectly good `$DISPLAY` off to check it. Opened V4, whether the viewer should be a browser page instead, since a working Tcl/Tk is the only part of this that does not travel; deliberately not decided before the viewer has been used
- 2026-09-17 : the viewer's Python is pinned to 3.13 in its PEP 723 block, which is the fix for the `init.tcl` failure and replaces the wrong answer given an hour earlier. uv's standalone CPythons differ: the 3.13 builds carry Tcl/Tk 9.0 and work, while the build resolved on the other machine was linked against Tcl 8.6 with no 8.6 library files. The first response to that was to fall back to a system Python and an apt package, which gave up the property that made one copied file worth having; naming the version keeps it. Verified here that the pin resolves 3.13.14 and reaches a display error rather than a Tcl error, which is the signal that Tk initialised
- 2026-09-17 : the Python pin needed an upper bound as well as a floor, `>=3.13,<3.14`. `>=3.13` is satisfied by 3.14, so uv took the newest it could and resolved 3.14.2, which reports Tk 8.6 and cannot find a Tcl library its own distribution does not contain; that is the same failure the pin was meant to avoid. Reproduced locally by forcing each version rather than reasoning about it: 3.13 gives Tk 9.0 and reaches a display error, 3.14 does not. Third correction on the same question in a day, and the pattern is asserting a fix from one machine's resolution instead of pinning what was actually observed to work
- 2026-09-17 : the viewer opened a window and then aborted with `[xcb] Unknown sequence number` and an Xlib assertion. The reader thread was the cause without ever touching Tk: each redraw discards a `PhotoImage` whose finaliser calls into Tk, and a finaliser runs on whichever thread triggers the collection, so a socket read on the second thread could collect one and make X calls concurrently with the main loop. Removed the thread; the socket is polled from tkinter's own loop, which also made partial reads explicit, since a full frame is about a megabyte and TCP never delivers it at once. Five tests cover reassembly from fragments, and one asserts the file has no threads at all
- 2026-09-17 : the viewer is a browser page, not a tkinter window. V4 answered, V1 overturned. The tkinter version never ran on the machine it was written for: three Python pins and a threading fix, each asserted from this box's behaviour, each failing differently there. What settled it was a probe containing no klide at all, in which a bare `Tk()` succeeded and `root.update()` aborted inside Xlib. It also reported Tcl 8.6.14 with the threaded notifier on, which retracts the previous day's diagnosis: the thread making X calls was Tcl's, not the one I removed, and the version pin had never been selecting the Tcl I said it was. A browser removes the class of failure rather than the instance. The file is still standard library only and still imports no klide, so AD5 is unaffected; what changes is that the machine with the screen now needs a browser and a forwarded port instead of an interpreter. One thing is lost and recorded rather than glossed: a browser cannot measure the monitor, so true physical size is calibrated against a ruler with a 100 mm bar on the page, and the status line says "assumed" until it is. The JavaScript is the one part not covered by a test, because there is no browser and no JS runtime here; the server, the protocol and the event stream are driven end to end over real HTTP
- 2026-09-17 : the viewer's page can be driven from a script, `scripts/drive.py`, in Chromium or Firefox, with the browser console, the viewer's log and the host's log folded into one stream. Added because the first thing a person reported was "the page buttons do not do anything" and nothing in the system could say where the event stopped. It found the answer in one run: page forward at the live tail is correctly ignored, and the page showed what a broken button shows, which is nothing. The status line now distinguishes sent, failed, and no change. Both engines run the input path correctly, so three rounds of server-side probing had been looking in the wrong place. Playwright is a dev dependency and the browsers live in `~/.cache/ms-playwright`; the box note is `~/repos/plans/2026-09-17_00_install_playwright_chromium.md`. Deliberately not a gate, which is V5
- 2026-09-17 : the Tcl versions, measured rather than asserted. Every uv-managed interpreter on this box reports Tcl/Tk 9.0 (3.11, 3.12, 3.13) and uv's 3.14 has no tkinter at all, while the other machine reported 8.6.14 under the same `>=3.13,<3.14` pin. A uv-managed 3.13 cannot report 8.6, so `uv run` there used an interpreter already installed on that machine, which links the system Tcl. The pin constrained the version and never selected the build, which is exactly what it was claimed three times to be doing. The viewer is a browser page now so none of it matters to the code, but the reasoning error does: a version number was read and a build inferred from it
- 2026-09-17 : frames go to the browser as PNG, not as raw pixels, after a person reported four to five seconds a press over an SSH tunnel. Measured before changing anything: the host renders, diffs and encodes in under 0.2s and a press-to-paint round trip on this box is 0.35s, so the time was the link carrying 2.8 MB of base64 for a full screen. The same screen is 24 KB as a PNG, 117x smaller, and at 5 Mbit/s that is 4.5s against 0.04s, which matches the reported delay closely enough to be the explanation. The browser decodes it natively, so the page's loop over two million pixels is gone too. The viewer writes the PNG from `zlib` and `struct` since it carries no dependencies, and the bytes are checked against Pillow in a test rather than against itself. Verified in both engines by reading the canvas back: exactly the sixteen levels, no colour management shift. The cause of the original mistake is worth more than the fix, which is that this was designed where the viewer and the browser were the same machine and the link was free
- 2026-09-17 : "the font looks janky" turned into one real defect and two ruled-out suspects. The rendering at panel resolution is clean, checked by magnifying klide's own frame: sixteen greys, evenly antialiased. The browser's default downscale was not it either, and replacing it with an area resample changed almost nothing in a magnified comparison. The defect was a second resample: the canvas backing store was an integer 459 pixels while its CSS width was set to the fractional ideal 459.27, so the compositor rescaled it again by 1.0006, too small to read as a size change and enough to make stems uneven. Now sized from the backing store, one canvas pixel to one device pixel, verified in the page. The panel and the displayed canvas are separate as a result, which also makes the viewer correct on a HiDPI screen. What is left is physics: 300 ppi shown at 109 ppi has lost two thirds of its detail, so size is judged at true size and sharpness at 1:1
- 2026-09-17 : the viewer's first sitting produced five findings and all five are fixed: dead-looking buttons, four seconds a press, true size nine per cent small, jagged text, and the density box not re-fitting. All five are about the instrument, not about the design it exists to judge, so phase 5 is not done. Marking it done because the viewer works would be the self-referential move AD9 is about. The rendering quality is accepted provisionally, confirmed clean at 1:1 and much improved at true size, and parked until there is a real panel to look at

