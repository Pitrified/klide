--- a/src/klide/render.py
+++ b/src/klide/render.py
@@ -20,9 +20,11 @@ from klide.panel import Panel
-MARGIN = 24
-FONT_SIZE = 26
-LINE_HEIGHT = 34
+BODY_POINTS = 11.0
+LINE_SPACING = 1.45
+MARGIN_POINTS = 11.5

 def render_text(text, panel):
-    font = ImageFont.load_default(size=FONT_SIZE)
+    font_px = panel.points_to_pixels(BODY_POINTS)
+    font = load(Face.BODY, font_px)
     return draw(text, font)
